<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TmaSkuWarehouseRelDao">
	<resultMap id="TmaSkuWarehouseRelResult" type="io.vitess.model.sqlmap.TmaSkuWarehouseRel">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tmaSkuWarehouseRelColumns">
	    <![CDATA[
	        	ID as id,
	        	SHOP_ID as shopId,
	        	SKU_CODE as skuCode,
	        	WH_CODE as whCode,
	        	SKU_TYPE as skuType
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TmaSkuWarehouseRel"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_ma_sku_warehouse_rel (
		        id,
		        	SHOP_ID ,
		        	SKU_CODE ,
		        	WH_CODE ,
		        	SKU_TYPE 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{shopId,javaType=Long,jdbcType=BIGINT} ,
		        	#{skuCode,javaType=String,jdbcType=VARCHAR} ,
		        	#{whCode,javaType=String,jdbcType=VARCHAR} ,
		        	#{skuType,javaType=String,jdbcType=VARCHAR} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TmaSkuWarehouseRel">
	    <![CDATA[
	        UPDATE t_ma_sku_warehouse_rel SET
		        SHOP_ID = #{shopId,javaType=Long,jdbcType=BIGINT} ,
		        SKU_CODE = #{skuCode,javaType=String,jdbcType=VARCHAR} ,
		        WH_CODE = #{whCode,javaType=String,jdbcType=VARCHAR} ,
		        SKU_TYPE = #{skuType,javaType=String,jdbcType=VARCHAR} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_ma_sku_warehouse_rel where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_ma_sku_warehouse_rel 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TmaSkuWarehouseRelResult" flushCache="false">
			select <include refid="tmaSkuWarehouseRelColumns" />
		    <![CDATA[
			    from t_ma_sku_warehouse_rel 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TmaSkuWarehouseRelResult" flushCache="false">
			select <include refid="tmaSkuWarehouseRelColumns" />
			    from t_ma_sku_warehouse_rel 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tmaSkuWarehouseRelDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="shopId!=null">
					and SHOP_ID = #{shopId}
				</if>
	       
				<if test="skuCode!=null">
					and SKU_CODE = #{skuCode}
				</if>
	       
				<if test="whCode!=null">
					and WH_CODE = #{whCode}
				</if>
	       
				<if test="skuType!=null">
					and SKU_TYPE = #{skuType}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_ma_sku_warehouse_rel 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_ma_sku_warehouse_rel 
		<include refid="tmaSkuWarehouseRelDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_ma_sku_warehouse_rel 
		<include refid="tmaSkuWarehouseRelDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TmaSkuWarehouseRelResult">
    	select <include refid="tmaSkuWarehouseRelColumns" />
	    from t_ma_sku_warehouse_rel 
		<include refid="tmaSkuWarehouseRelDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TmaSkuWarehouseRelResult">
    	select <include refid="tmaSkuWarehouseRelColumns" />
	    from t_ma_sku_warehouse_rel 
		<include refid="tmaSkuWarehouseRelDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TmaSkuWarehouseRelResult">
    	select <include refid="tmaSkuWarehouseRelColumns" />
	    from t_ma_sku_warehouse_rel 
		<include refid="tmaSkuWarehouseRelDynamicWhere"/>
		
    </select>

	
</mapper>
