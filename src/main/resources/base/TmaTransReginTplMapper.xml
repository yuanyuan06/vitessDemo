<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TmaTransReginTplDao">
	<resultMap id="TmaTransReginTplResult" type="io.vitess.model.sqlmap.TmaTransReginTpl">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tmaTransReginTplColumns">
	    <![CDATA[
	        	ID as id,
	        	CODE as code,
	        	CREATE_TIME as createTime,
	        	LAST_MODIFY_TIME as lastModifyTime,
	        	LAST_MODIFY_USER as lastModifyUser,
	        	NAME as name,
	        	REMARK as remark,
	        	VERSION as version,
	        	TRANSPORTATOR_ID as transportatorId
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TmaTransReginTpl"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_ma_trans_regin_tpl (
		        id,
		        	CODE ,
		        	CREATE_TIME ,
		        	LAST_MODIFY_TIME ,
		        	LAST_MODIFY_USER ,
		        	NAME ,
		        	REMARK ,
		        	VERSION ,
		        	TRANSPORTATOR_ID 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{code,javaType=String,jdbcType=VARCHAR} ,
		        	#{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        	#{lastModifyTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        	#{lastModifyUser,javaType=String,jdbcType=VARCHAR} ,
		        	#{name,javaType=String,jdbcType=VARCHAR} ,
		        	#{remark,javaType=String,jdbcType=VARCHAR} ,
		        	#{version,javaType=Integer,jdbcType=INTEGER} ,
		        	#{transportatorId,javaType=Long,jdbcType=BIGINT} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TmaTransReginTpl">
	    <![CDATA[
	        UPDATE t_ma_trans_regin_tpl SET
		        CODE = #{code,javaType=String,jdbcType=VARCHAR} ,
		        CREATE_TIME = #{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        LAST_MODIFY_TIME = #{lastModifyTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        LAST_MODIFY_USER = #{lastModifyUser,javaType=String,jdbcType=VARCHAR} ,
		        NAME = #{name,javaType=String,jdbcType=VARCHAR} ,
		        REMARK = #{remark,javaType=String,jdbcType=VARCHAR} ,
		        VERSION = #{version,javaType=Integer,jdbcType=INTEGER} ,
		        TRANSPORTATOR_ID = #{transportatorId,javaType=Long,jdbcType=BIGINT} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_ma_trans_regin_tpl where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_ma_trans_regin_tpl 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TmaTransReginTplResult" flushCache="false">
			select <include refid="tmaTransReginTplColumns" />
		    <![CDATA[
			    from t_ma_trans_regin_tpl 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TmaTransReginTplResult" flushCache="false">
			select <include refid="tmaTransReginTplColumns" />
			    from t_ma_trans_regin_tpl 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tmaTransReginTplDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="code!=null">
					and CODE = #{code}
				</if>
	       
				<if test="createTime!=null">
					and CREATE_TIME = #{createTime}
				</if>
	       
				<if test="lastModifyTime!=null">
					and LAST_MODIFY_TIME = #{lastModifyTime}
				</if>
	       
				<if test="lastModifyUser!=null">
					and LAST_MODIFY_USER = #{lastModifyUser}
				</if>
	       
				<if test="name!=null">
					and NAME = #{name}
				</if>
	       
				<if test="remark!=null">
					and REMARK = #{remark}
				</if>
	       
				<if test="version!=null">
					and VERSION = #{version}
				</if>
	       
				<if test="transportatorId!=null">
					and TRANSPORTATOR_ID = #{transportatorId}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_ma_trans_regin_tpl 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_ma_trans_regin_tpl 
		<include refid="tmaTransReginTplDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_ma_trans_regin_tpl 
		<include refid="tmaTransReginTplDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TmaTransReginTplResult">
    	select <include refid="tmaTransReginTplColumns" />
	    from t_ma_trans_regin_tpl 
		<include refid="tmaTransReginTplDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TmaTransReginTplResult">
    	select <include refid="tmaTransReginTplColumns" />
	    from t_ma_trans_regin_tpl 
		<include refid="tmaTransReginTplDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TmaTransReginTplResult">
    	select <include refid="tmaTransReginTplColumns" />
	    from t_ma_trans_regin_tpl 
		<include refid="tmaTransReginTplDynamicWhere"/>
		
    </select>

	
</mapper>
