<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TmqSoPackingInfoLogDao">
	<resultMap id="TmqSoPackingInfoLogResult" type="io.vitess.model.sqlmap.TmqSoPackingInfoLog">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tmqSoPackingInfoLogColumns">
	    <![CDATA[
	        	ID as id,
	        	MEMO as memo,
	        	PI_LEVEL as piLevel,
	        	TYPE as type,
	        	SO_LINE_LOG_ID as soLineLogId,
	        	SO_LOG_ID as soLogId,
	        	SHOP_ID as shopId
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TmqSoPackingInfoLog"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_mq_so_packing_info_log (
		        id,
		        	MEMO ,
		        	PI_LEVEL ,
		        	TYPE ,
		        	SO_LINE_LOG_ID ,
		        	SO_LOG_ID ,
		        	SHOP_ID 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{memo,javaType=String,jdbcType=VARCHAR} ,
		        	#{piLevel,javaType=Integer,jdbcType=INTEGER} ,
		        	#{type,javaType=Integer,jdbcType=INTEGER} ,
		        	#{soLineLogId,javaType=Long,jdbcType=BIGINT} ,
		        	#{soLogId,javaType=Long,jdbcType=BIGINT} ,
		        	#{shopId,javaType=Long,jdbcType=BIGINT} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TmqSoPackingInfoLog">
	    <![CDATA[
	        UPDATE t_mq_so_packing_info_log SET
		        MEMO = #{memo,javaType=String,jdbcType=VARCHAR} ,
		        PI_LEVEL = #{piLevel,javaType=Integer,jdbcType=INTEGER} ,
		        TYPE = #{type,javaType=Integer,jdbcType=INTEGER} ,
		        SO_LINE_LOG_ID = #{soLineLogId,javaType=Long,jdbcType=BIGINT} ,
		        SO_LOG_ID = #{soLogId,javaType=Long,jdbcType=BIGINT} ,
		        SHOP_ID = #{shopId,javaType=Long,jdbcType=BIGINT} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_mq_so_packing_info_log where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_mq_so_packing_info_log 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TmqSoPackingInfoLogResult" flushCache="false">
			select <include refid="tmqSoPackingInfoLogColumns" />
		    <![CDATA[
			    from t_mq_so_packing_info_log 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TmqSoPackingInfoLogResult" flushCache="false">
			select <include refid="tmqSoPackingInfoLogColumns" />
			    from t_mq_so_packing_info_log 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tmqSoPackingInfoLogDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="memo!=null">
					and MEMO = #{memo}
				</if>
	       
				<if test="piLevel!=null">
					and PI_LEVEL = #{piLevel}
				</if>
	       
				<if test="type!=null">
					and TYPE = #{type}
				</if>
	       
				<if test="soLineLogId!=null">
					and SO_LINE_LOG_ID = #{soLineLogId}
				</if>
	       
				<if test="soLogId!=null">
					and SO_LOG_ID = #{soLogId}
				</if>
	       
				<if test="shopId!=null">
					and SHOP_ID = #{shopId}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_mq_so_packing_info_log 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_mq_so_packing_info_log 
		<include refid="tmqSoPackingInfoLogDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_mq_so_packing_info_log 
		<include refid="tmqSoPackingInfoLogDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TmqSoPackingInfoLogResult">
    	select <include refid="tmqSoPackingInfoLogColumns" />
	    from t_mq_so_packing_info_log 
		<include refid="tmqSoPackingInfoLogDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TmqSoPackingInfoLogResult">
    	select <include refid="tmqSoPackingInfoLogColumns" />
	    from t_mq_so_packing_info_log 
		<include refid="tmqSoPackingInfoLogDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TmqSoPackingInfoLogResult">
    	select <include refid="tmqSoPackingInfoLogColumns" />
	    from t_mq_so_packing_info_log 
		<include refid="tmqSoPackingInfoLogDynamicWhere"/>
		
    </select>

	
</mapper>
