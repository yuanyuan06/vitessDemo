<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TmaWhInvStatusDao">
	<resultMap id="TmaWhInvStatusResult" type="io.vitess.model.sqlmap.TmaWhInvStatus">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tmaWhInvStatusColumns">
	    <![CDATA[
	        	ID as id,
	        	CODE as code,
	        	NAME as name,
	        	TYPE as type
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TmaWhInvStatus"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_ma_wh_inv_status (
		        id,
		        	CODE ,
		        	NAME ,
		        	TYPE 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{code,javaType=String,jdbcType=VARCHAR} ,
		        	#{name,javaType=String,jdbcType=VARCHAR} ,
		        	#{type,javaType=Integer,jdbcType=TINYINT} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TmaWhInvStatus">
	    <![CDATA[
	        UPDATE t_ma_wh_inv_status SET
		        CODE = #{code,javaType=String,jdbcType=VARCHAR} ,
		        NAME = #{name,javaType=String,jdbcType=VARCHAR} ,
		        TYPE = #{type,javaType=Integer,jdbcType=TINYINT} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_ma_wh_inv_status where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_ma_wh_inv_status 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TmaWhInvStatusResult" flushCache="false">
			select <include refid="tmaWhInvStatusColumns" />
		    <![CDATA[
			    from t_ma_wh_inv_status 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TmaWhInvStatusResult" flushCache="false">
			select <include refid="tmaWhInvStatusColumns" />
			    from t_ma_wh_inv_status 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tmaWhInvStatusDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="code!=null">
					and CODE = #{code}
				</if>
	       
				<if test="name!=null">
					and NAME = #{name}
				</if>
	       
				<if test="type!=null">
					and TYPE = #{type}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_ma_wh_inv_status 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_ma_wh_inv_status 
		<include refid="tmaWhInvStatusDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_ma_wh_inv_status 
		<include refid="tmaWhInvStatusDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TmaWhInvStatusResult">
    	select <include refid="tmaWhInvStatusColumns" />
	    from t_ma_wh_inv_status 
		<include refid="tmaWhInvStatusDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TmaWhInvStatusResult">
    	select <include refid="tmaWhInvStatusColumns" />
	    from t_ma_wh_inv_status 
		<include refid="tmaWhInvStatusDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TmaWhInvStatusResult">
    	select <include refid="tmaWhInvStatusColumns" />
	    from t_ma_wh_inv_status 
		<include refid="tmaWhInvStatusDynamicWhere"/>
		
    </select>

	
</mapper>
