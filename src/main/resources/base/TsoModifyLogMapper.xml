<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TsoModifyLogDao">
	<resultMap id="TsoModifyLogResult" type="io.vitess.model.sqlmap.TsoModifyLog">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tsoModifyLogColumns">
	    <![CDATA[
	        	ID as id,
	        	CONTENT as content,
	        	CREATE_TIME as createTime,
	        	ORDER_CODE as orderCode,
	        	SHOP_ID as shopId
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TsoModifyLog"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_so_modify_log (
		        id,
		        	CONTENT ,
		        	CREATE_TIME ,
		        	ORDER_CODE ,
		        	SHOP_ID 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{content,javaType=String,jdbcType=LONGVARCHAR} ,
		        	#{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        	#{orderCode,javaType=String,jdbcType=VARCHAR} ,
		        	#{shopId,javaType=Long,jdbcType=BIGINT} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TsoModifyLog">
	    <![CDATA[
	        UPDATE t_so_modify_log SET
		        CONTENT = #{content,javaType=String,jdbcType=LONGVARCHAR} ,
		        CREATE_TIME = #{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        ORDER_CODE = #{orderCode,javaType=String,jdbcType=VARCHAR} ,
		        SHOP_ID = #{shopId,javaType=Long,jdbcType=BIGINT} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_so_modify_log where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_so_modify_log 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TsoModifyLogResult" flushCache="false">
			select <include refid="tsoModifyLogColumns" />
		    <![CDATA[
			    from t_so_modify_log 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TsoModifyLogResult" flushCache="false">
			select <include refid="tsoModifyLogColumns" />
			    from t_so_modify_log 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tsoModifyLogDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="content!=null">
					and CONTENT = #{content}
				</if>
	       
				<if test="createTime!=null">
					and CREATE_TIME = #{createTime}
				</if>
	       
				<if test="orderCode!=null">
					and ORDER_CODE = #{orderCode}
				</if>
	       
				<if test="shopId!=null">
					and SHOP_ID = #{shopId}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_so_modify_log 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_so_modify_log 
		<include refid="tsoModifyLogDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_so_modify_log 
		<include refid="tsoModifyLogDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TsoModifyLogResult">
    	select <include refid="tsoModifyLogColumns" />
	    from t_so_modify_log 
		<include refid="tsoModifyLogDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TsoModifyLogResult">
    	select <include refid="tsoModifyLogColumns" />
	    from t_so_modify_log 
		<include refid="tsoModifyLogDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TsoModifyLogResult">
    	select <include refid="tsoModifyLogColumns" />
	    from t_so_modify_log 
		<include refid="tsoModifyLogDynamicWhere"/>
		
    </select>

	
</mapper>
