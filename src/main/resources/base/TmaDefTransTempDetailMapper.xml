<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="io.vitess.dao.sqlmap.TmaDefTransTempDetailDao">
	<resultMap id="TmaDefTransTempDetailResult" type="io.vitess.model.sqlmap.TmaDefTransTempDetail">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="tmaDefTransTempDetailColumns">
	    <![CDATA[
	        	ID as id,
	        	PROVINCE as province,
	        	REACHABLE_AREA as reachableArea,
	        	UN_REACHABLE_AREA as unReachableArea,
	        	TEMP_ID as tempId,
	        	TRANS_ID as transId,
	        	VERSION as version
	    ]]>
	</sql>

		<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
		<insert id="insert" parameterType="io.vitess.model.sqlmap.TmaDefTransTempDetail"
				
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        t_ma_def_trans_temp_detail (
		        id,
		        	PROVINCE ,
		        	REACHABLE_AREA ,
		        	UN_REACHABLE_AREA ,
		        	TEMP_ID ,
		        	TRANS_ID ,
		        	VERSION 
		        ) VALUES (
		        #{id,javaType=java.lang.Long,jdbcType=BIGINT} ,
		        	#{province,javaType=String,jdbcType=VARCHAR} ,
		        	#{reachableArea,javaType=String,jdbcType=VARCHAR} ,
		        	#{unReachableArea,javaType=String,jdbcType=VARCHAR} ,
		        	#{tempId,javaType=Long,jdbcType=BIGINT} ,
		        	#{transId,javaType=Long,jdbcType=BIGINT} ,
		        	#{version,javaType=Integer,jdbcType=INTEGER} 
		        )
		    ]]>

		</insert>
		

		<update id="update" parameterType="io.vitess.model.sqlmap.TmaDefTransTempDetail">
	    <![CDATA[
	        UPDATE t_ma_def_trans_temp_detail SET
		        PROVINCE = #{province,javaType=String,jdbcType=VARCHAR} ,
		        REACHABLE_AREA = #{reachableArea,javaType=String,jdbcType=VARCHAR} ,
		        UN_REACHABLE_AREA = #{unReachableArea,javaType=String,jdbcType=VARCHAR} ,
		        TEMP_ID = #{tempId,javaType=Long,jdbcType=BIGINT} ,
		        TRANS_ID = #{transId,javaType=Long,jdbcType=BIGINT} ,
		        VERSION = #{version,javaType=Integer,jdbcType=INTEGER} 
	        WHERE 
		        ID = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from t_ma_def_trans_temp_detail where
		        ID = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from t_ma_def_trans_temp_detail 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="TmaDefTransTempDetailResult" flushCache="false">
			select <include refid="tmaDefTransTempDetailColumns" />
		    <![CDATA[
			    from t_ma_def_trans_temp_detail 
		        where 
		        ID = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="TmaDefTransTempDetailResult" flushCache="false">
			select <include refid="tmaDefTransTempDetailColumns" />
			    from t_ma_def_trans_temp_detail 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="tmaDefTransTempDetailDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and ID = #{id}
				</if>
	       
				<if test="province!=null">
					and PROVINCE = #{province}
				</if>
	       
				<if test="reachableArea!=null">
					and REACHABLE_AREA = #{reachableArea}
				</if>
	       
				<if test="unReachableArea!=null">
					and UN_REACHABLE_AREA = #{unReachableArea}
				</if>
	       
				<if test="tempId!=null">
					and TEMP_ID = #{tempId}
				</if>
	       
				<if test="transId!=null">
					and TRANS_ID = #{transId}
				</if>
	       
				<if test="version!=null">
					and VERSION = #{version}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from t_ma_def_trans_temp_detail 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from t_ma_def_trans_temp_detail 
		<include refid="tmaDefTransTempDetailDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from t_ma_def_trans_temp_detail 
		<include refid="tmaDefTransTempDetailDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="TmaDefTransTempDetailResult">
    	select <include refid="tmaDefTransTempDetailColumns" />
	    from t_ma_def_trans_temp_detail 
		<include refid="tmaDefTransTempDetailDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="TmaDefTransTempDetailResult">
    	select <include refid="tmaDefTransTempDetailColumns" />
	    from t_ma_def_trans_temp_detail 
		<include refid="tmaDefTransTempDetailDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="TmaDefTransTempDetailResult">
    	select <include refid="tmaDefTransTempDetailColumns" />
	    from t_ma_def_trans_temp_detail 
		<include refid="tmaDefTransTempDetailDynamicWhere"/>
		
    </select>

	
</mapper>
